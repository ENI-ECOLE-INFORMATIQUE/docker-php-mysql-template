services:

  php:
    build: .
    container_name: php_apache
    ports:
      - "8080:80"

    volumes:
      - ./coursPHP:/var/www/html
      - ./php/php.ini:/usr/local/etc/php/php.ini

    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    container_name: mysql

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: root

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql
      
      # Scripts SQL automatiques
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./mysql/BDLegumes.sql:/docker-entrypoint-initdb.d/02-BDLegumes.sql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin

    restart: always

    depends_on:
      - mysql

    ports:
      - "8081:80"

    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: root

volumes:
  mysql_data:
